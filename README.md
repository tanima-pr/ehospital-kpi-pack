# EHospital KPI SQL Pack

This repo contains curated KPI queries used by the backend agent.

## Folder structure
- queries/: one KPI per .sql file

## Usage
Run queries in MySQL Workbench or via backend scripts.
