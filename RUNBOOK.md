# Runbook (DB)

## Step 1 — Create curated views
Open `queries/00_create_curated_views.sql` in MySQL Workbench and run it.

## Step 2 — Run KPIs
Run any KPI file in `queries/` (one KPI per file).

## Step 3 — Data quality checks
Run `queries/99_quality_checks.sql`.